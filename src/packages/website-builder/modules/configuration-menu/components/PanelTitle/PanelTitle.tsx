import {H2} from '../../../../../diamond-ui';

export const PanelTitle = ({title}) => {
    return (
        <div style={{padding: '6px 6px 0 33px'}}>
            <H2>{title}</H2>
        </div>
    );
};
